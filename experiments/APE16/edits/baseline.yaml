
dropout_rate: 0.5
cell_size: 128
attn_size: 128
embedding_size: 64

layers: 1
bidir: True
use_lstm: True

max_output_len: 45   # 99% coverage
max_input_len: 36
weight_scale: 0.1

data_dir: experiments/APE16/edits/data_new
model_dir: experiments/APE16/edits/baseline_small

batch_size: 32

optimizer: 'sgd'
#learning_rate: 0.0003
learning_rate: 1.0
learning_rate_decay_factor: 0.95
decay_every_n_epoch: 1
decay_after_n_epoch: 5

steps_per_checkpoint: 400
steps_per_eval: 400
score_function: corpus_scores_ter

max_gradient_norm: 1.0
max_epochs: 80
batch_mode: 'standard'
read_ahead: 10

encoders:
  - name: de
    ext: mt
    align_edits: True
    attention_type: 'local'
    attention_window_size: 0

decoder:
    name: de
    ext: edits
    cell_size: 64
    embedding_size: 64
    layers: 1
    #pred_name: edits
    #pred_ext: edits
    #tie_embeddings: True
    pred_edits: True
    #pred_ext: edits
    #pred_name: edits
    pred_state: True
    pred_input: 'ops'
    no_del_update: True
    tie_embeddings: True
